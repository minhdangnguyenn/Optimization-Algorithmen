import { AreaDescendingStrategy } from "./area-descending";
import { HeightDescendingStrategy } from "./heigh-descending";

export { AreaDescendingStrategy, HeightDescendingStrategy };

export enum SelectionStrategy {
    AREA = "area",
    HEIGHT = "height",
}
